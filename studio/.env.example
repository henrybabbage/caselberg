SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
# Optional (deploy / org tools)
# SANITY_STUDIO_ORGANIZATION=

# For `npm run seed` only — must be an API token with Editor (or Developer) access, not Viewer
# Create at: https://www.sanity.io/manage → Project → API → Tokens
SANITY_API_WRITE_TOKEN=
